# 锁定版订阅盒对齐解锁版（2026-07-31 20:08）

## Adam 反馈
解锁版和未解锁版相同的模块可以高重合度，**锁定版的底部订阅盒**就和**解锁版**不一样，希望以解锁版为准

## 修复

锁定版 `app/result/page.tsx` 的订阅盒区块，从「订阅服务」旧样式 → 改用与解锁版完全相同的卡片结构。

### 改动点

| 元素 | 之前 | 之后（对齐解锁版）|
|---|---|---|
| 区块标题 | 订阅服务 | **一封信**（解锁版同款） |
| 区块副标 | 把今天的答案，装进一支香 | **完整版已解锁 · 把今天的故事，装进一支香** |
| 卡片背景 | `bg-white` + `opacity-90` | **#FAF3EA**（cream，无 opacity）|
| 卡片边框 | `border-amber-200` | **1px solid #F0D8B6** |
| 卡片内边距 | `p-6` (24px) | **p-5** (20px) |
| 右上角徽章 | 两个 chip 横排（新品 + 实物寄送）| **单个 chip「✦ 实物寄送」**（右上角 3/3）|
| 标题 | `text-lg` | `text-[18px]` + 内联「新品」chip（标题内）|
| 副标 | 「{subscriptionCfg.subtitle}」 | **「为你而调的序章」**（硬编码）|
| 价签 | text-[32px] 数字 | **text-[32px]** 一致 |
| 省标签 | 无 | **新增「省 ¥X」** #C2410C 橙色（橙味主色）|
| bullet list | `·` 圆点 | **✓** 加粗勾 |
| 列表项 | space-y-1.5（15 项间距）| **py-1**（每个 li 上下间距）|
| 「即将开放」文字 | 「订阅盒即将开放，敬请期待」独立段 | **移除**（点击 alert 弹） |
| 按钮样式 | 旧 `border-amber-800` | **新「1.5px solid #8B5E3C」+ 透明背景** |
| 按钮交互 | `setPayKey('subscriptionBox')` | **alert('订阅盒即将开放，敬请期待')**（点击不跳支付，因为已开放关闭）|

### 关键发现

订阅盒目前「即将开放」——所以**不应该**调起 `setPayKey`。之前锁定版点了会进支付流程，这是 bug。现在对齐解锁版行为：点了直接弹提示。

### 解锁版验证

解锁版（`components/UnlockedContent.tsx`）订阅盒代码**未修改**——只锁定版跟随解锁版样式 + 行为对齐。

## 构建状态

- ✅ build 4.8s, 11/11 路由静态
- ✅ 3456 UP, /result?previewPaid=1 200

## Adam 下一步

刷新 `http://192.168.10.15:3456/result`（锁定态），查看底部订阅盒：
- 卡片现在是 cream 背景（不是白色）
- 标题统一「一封信」
- 新增「省 ¥70」橙色省价签
- 点击按钮只弹提示，不再走支付
- 整个卡片视觉与解锁版**像素级一致**

预览链接：
- 锁定版：http://192.168.10.15:3456/result
- 解锁版：http://192.168.10.15:3456/result?previewPaid=1
- 解锁版默认人格：http://192.168.10.15:3456/preview

对比两版底部订阅盒：样式一致，但锁定版显示「订阅盒即将开放」交互差异（不会进支付）。