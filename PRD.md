# Crush 香鉴 · 产品需求文档（PRD）

> 文档版本：v1.0（初版，基于对话历史 + 项目代码逆向整理）
> 整理视角：资深产品经理（PM）
> 数据来源：对话历史（vibe coding 过程）+ 当前项目代码（Next.js 16 工程）真实读取
> 说明：本 PRD 所有功能、技术栈、页面与交互均来自代码实证；凡代码/对话中读不到的产品意图、商业数据，一律标注 **【待确认】**。

---

## 一、产品概述

### 1.1 产品一句话定义
**Crush 香鉴** 是一款面向移动端的「嗅觉人格测试」H5 轻应用：用户通过 10 道沉浸式情境题，被匹配到 16 种「灵魂人格」之一，并基于人格的香调偏好向量，从香水库中推荐 3 款分层香水（本命香 / 进阶香 / 尝试香）。

### 1.2 产品主张（Slogan，代码实证）
- 主标语：**「你的灵魂，藏在哪种香气里？」**
- 副标语（落地页）：「每个人身上，都藏着一种独一无二的香气。找到它，就找到了自己。」
- 数据条（落地页 Hero）：**16 人格 · {TOTAL_PERFUMES} 香水 · 10 题**（香水数为动态读取 `Object.keys(PERFUMES).length`）。
- 测试说明（About 区）：「融合嗅觉心理学与人格特质理论，通过 10 道沉浸式情境题，探索感官偏好与内心世界，匹配 16 种灵魂人格之一，并找到那支与你共振的本命香。」

### 1.3 产品形态
- **纯前端、零后端、零数据库**：所有计算（人格判定、推荐、匹配、抽签）均在浏览器/SSR 内确定性完成；用户状态仅存于 `localStorage`。
- **跨平台 H5**：以移动端（≤430px 宽）为主，可经 Vercel 部署为公开站点，分享图经服务端 SVG→PNG 渲染。

### 1.4 补充背景（PM 需补全，注明确定性）

| 背景项 | 填写内容 | 确定性 |
|---|---|---|
| **这个产品是给谁用的** | 对自身气味审美好奇、乐于做人格/心理类测试的年轻女性向用户（落地页头像占位名「珊/落/渊/舟/砚」、social proof 文案「90% 的人说像被读懂了一次」暗示偏情感向、社交分享型人群）；同时具备「轻裂变拉新」设计（邀请码、CP 共振卡），目标含泛社交传播用户。 | 部分确定（人群画像由 UI 文案/占位推断） |
| **当初为什么要做（业务动机）** | 【待确认】代码与对话中未见明确商业立项文档；从功能结构看，产品定位为「内容种草 + 轻付费 + 社交裂变」的香水导流/品牌认知工具（落地页即「CRUSH FRAGRANCE」品牌符号，支付墙锁进阶解读）。具体立项背景（如是否为某香水品牌/电商导流）【待确认】。 | 不确定 |
| **最看重的商业/用户目标** | 用户目标：**降低「选香」门槛、提供情绪价值与自我认知愉悦感、制造可分享的社交货币**（分享图、CP 卡、香气历）。商业目标：**通过支付墙（¥20.9 引流价 / ¥29.9 主解锁价）变现解读深度**；【待确认】是否以电商抽佣/品牌合作为终极变现，因支付接口当前为预留空实现（`initiatePayment()` 返回 `{ok:false}`），尚无真实交易数据。 | 用户目标确定；商业目标部分推测 |

---

## 二、业务目标与成功指标

> 说明：以下指标基线（baseline）多为 **【待确认】** —— 代码中仅有落地页写死的社交证明数字「已有 12,047 人完成」（疑为占位/静态文案，非真实埋点），且无任何真实埋点/营收数据可读。指标目标值为 PM 建议值，需以真实上线数据校准。

### 2.1 北极星指标（North Star）
| 指标 | 定义 | 基线 | 目标 |
|---|---|---|---|
| **完成测试并生成结果页的 UV 占比** | 进入测试漏斗 → 完成 10 题 → 到达结果页的用户比例 | 【待确认】 | 【待确认】建议 ≥ 60% |
| **结果页分享率**（生成分享图/复制链接 UV ÷ 结果页 UV） | 衡量「社交货币」成立与否的核心 | 【待确认】 | 【待确认】建议 ≥ 25% |

### 2.2 驱动指标（Drivers）
| 指标 | 说明 | 基线 | 目标 |
|---|---|---|---|
| 落地页 → 开始测试 CTR | 主 CTA「开始寻找我的本命香」点击率 | 【待确认】 | 【待确认】 |
| 测试完成率（10 题完卷率） | 受「情境题 + 转场 + 正在调香」体验影响 | 【待确认】 | 【待确认】 |
| 好友匹配发起率 | 结果页引导至 `/friend` 或邀请他人回流的比例 | 【待确认】 | 【待确认】 |
| 今日香签日活留存（香气历连续天数） | 连续访问抽签的天数分布 | 【待确认】 | 【待确认】建议次日留存 ≥ 30% |
| 支付墙转化率 | 到达解锁页 → 完成解锁（含邀请 3 人享折扣）比例 | 【待确认，且当前支付未接通】 | 【待确认】 |

### 2.3 健康指标（Guardrails）
| 指标 | 说明 | 红线 |
|---|---|---|
| 分享图渲染成功率 | `/api/share-card` 返回 200 比例（satori/sharp 环节） | 【待确认】建议 ≥ 99% |
| 内容合规拦截率 | 宜忌/签文是否触发平台审核限流 | 不应出现迷信/医疗/财务暗示（见第十章） |
| 首屏性能 | LCP / 可交互时间（移动端弱网） | 【待确认】 |
| 数据口径一致性 | 香水总数在 README/代码/分享页文案三处不一致（151/225/110），需统一 | 必须修正后再对外 |

---

## 三、用户角色与用户故事

### 3.1 角色定义
| 角色 | 说明 |
|---|---|
| **好奇探索者（主用户）** | 想了解「自己是什么香气」的普通用户，动机是自我认知 + 趣味。 |
| **社交分享者** | 测完后主动生成分享图、发给好友做 CP 匹配、晒香气历的用户。 |
| **被邀请者（裂变下游）** | 通过好友邀请链接/二维码进入，完成测试并回流匹配。 |
| **（潜在）付费解锁者** | 想看进阶解读/完整报告的免费用户，经支付墙转化。 |

### 3.2 用户故事（格式：作为[角色]，我希望[行为]，以便[价值]）
1. 作为**好奇探索者**，我希望用 3 分钟做完 10 道情境题，以便快速得到「我属于哪种灵魂人格 + 本命香」。
2. 作为**好奇探索者**，我希望看到自己的人格六维香调雷达图（木质/清新/东方/美食/柑橘/花香），以便直观理解我的气味偏好构成。
3. 作为**好奇探索者**，我希望得到分层的 3 款香水（本命/进阶/尝试）而非单一答案，以便按预算自由选择。
4. 作为**社交分享者**，我希望一键生成带雷达图与三香的精美分享图（朋友圈 1:1 / 小红书 3:4），以便发朋友圈/小红书作为社交货币。
5. 作为**社交分享者**，我希望把链接发給好友做「香气伴侣」匹配，看到契合度评分与 CP 合香卡，以便制造互动话题。
6. 作为**被邀请者**，我希望点开好友的邀请链接直接看到 TA 的结果并一键自己也测，以便低门槛参与并回流匹配。
7. 作为**好奇探索者**，我希望每天能抽一支「今日香签」+ 今日宜忌 + 香气历打卡，以便形成每日回访习惯。
8. 作为**（潜在）付费解锁者**，我希望用「邀请 3 位好友」换取代价更低的解锁价（¥20.9 vs ¥29.9），以便用更低成本看完整解读。
9. 作为**好奇探索者**，我希望在 Result/CpCodex/分享卡等「官方视图」里看到 16 种人格一一对应的不重复示例香，以便浏览完整产品内容而不觉重复。

---

## 四、功能规格清单

> 优先级：P0（核心闭环，缺失则产品不成立）/ P1（重要体验）/ P2（增益）。

### 4.1 落地页（Landing）— `app/page.tsx`
- **描述**：品牌氛围首屏 + 16 人格胶囊预览 + 主 CTA + 社会证明 + About；顶部胶囊切换「测香 / 今日香签」双模式。
- **输入**：URL（首次访问或带 `?inv=`/`?cp=` 回流）；`localStorage` 中是否已测（`useMyTestStatus`）。
- **输出**：进入 `/question`（测香）或渲染 `DailyPanel`（今日香签）；已测用户展示其人格胶囊 + 「查看结果 / 重新测试」双按钮。
- **业务规则**：
  - 数据条显示 `16 / TOTAL_PERFUMES / 10`（香水数动态）。
  - 点击人格胶囊 → Bottom Sheet 展示人格示例（mbti · 主调 · 描述）→「查看该人格示例报告」跳 `/result?p=`。
  - 顶部模式切换：`test` 渲染测香区块；`daily` 渲染 `DailyPanel`。
- **边界异常**：`localStorage` 旧格式（存中文名）在 `/question` 进入时一次性迁移清除；重新测试会清除付费标记 `crushxiangjian_paid_level`。
- **优先级**：P0。

### 4.2 人格测试（Quiz）— `app/question/page.tsx` + `lib/branchingQuestions.ts`
- **描述**：10 道情境题，结构为 **7 道分支题（二叉树路径）+ 3 道校准题（cal1/cal2/cal3）**；分支题经二叉树走到叶子，再进入校准，最后由 `calculatePersonalityFromPath` 判定 16 人格之一。
- **输入**：用户逐题二选一 / 校准选择；进度持久化于 `localStorage`（`crushxiangjian_branch_progress`）。
- **输出**：写入 `personality_id`、`radar_scores`、`path_labels`、`calibration_choices`；跳转 `/result`（带 `?inv=` 则跳 `/friend`，带 `?cp=` 则带参回 `/result`）。
- **业务规则**：
  - 章节步进器显示「第 N 题 / 共 7 题」（校准题不额外计数），并展示情境标签（关于温度/距离/夜/留白/回忆/边界/野心、香气记忆）。
  - 题间 300ms 转场文案（`TRANSITION_COPY`）+「正在调香」加载页（1.6s 轮播嗅觉暗示）。
  - 支持「上一题」回退、「跳过」用默认路径直接出结果。
  - 路径可视化：顶部横向展示已选路径标签（emoji + 文案，去重）。
- **边界异常**：`localStorage` 恢复出的 `currentQuestionId` 失效时按 phase 回退首题，避免空白页；旧中文名格式一次性迁移。
- **优先级**：P0。

### 4.3 结果页（Result）— `app/result/page.tsx`
- **描述**：展示人格解读 + 六维雷达图 + 三档推荐香水（本命香/进阶香/尝试香）+ 本命守护香 + 记忆场景 + 分享引导；含支付墙解锁区。
- **输入**：`?p=`（示例人格）或 `localStorage` 自测结果；`?cp=` 用于合香回流。
- **输出**：雷达图（`RadarChart`）、推荐列表（`getCalibratedRecommendations`）、守护香（`getGuardianPerfume`，匈牙利最优指派 16 款高端香之一）、分享图（`/api/share-card?scene=self`）、支付解锁（`PaymentModal`/`UnlockedContent`）。
- **业务规则**：
  - 推荐三档：本命香（premium 池 top1，匹配度映射至 85–95%）、进阶香（premium 池 ≥¥20/ml 高价香，真实分）、尝试香（budget 池 top1，含校准方向去重 + MMR 去相似）。
  - 雷达图六维中文标签映射：清新→水生、美食→馥奇（代码 `RADAR_DIM_LABELS`）。
  - 分享图锁定版（不含解锁内容）。
- **边界异常**：无 `localStorage` 自测结果且非示例访问时引导至测试；推荐算法对中文键雷达做 `normalizeRadarToEn` 防御（避免 NaN）。
- **优先级**：P0。

### 4.4 好友匹配（Friend Match）— `app/friend/FriendView.tsx` + `lib/friendMatch.ts` + `lib/cpResonance.ts`
- **描述**：邀请好友完成测试后，双方人格做契合度匹配，输出 0–100 分 + 5 档评级 + 共鸣气味 + 场景故事 + 互补解读 + **合香卡（CP Blend）**；并维护「我的香气伴侣榜」。
- **输入**：`?inv=`（邀请码）/ `?cp=A|B`（合香对）/ 手动输入；双方人格 id。
- **输出**：契合度评分（`calculateCompatibility`，共振 0.7 + 互补 0.3 + 基础 20）、评级（≥75 灵魂伴侣 / ≥65 天生一对 / ≥55 互补有趣 / ≥42 各有所爱 / else 气质迥异）、双人对比雷达、`CpBlendCard`、分享图（`scene=friend`）。
- **业务规则**：
  - 合香卡（`getCpResonance`）：双人香调按 6 音环（`diffTones` 0–3）计算「隔 N 调」，`diffTones=0` 显示「同调」；生成确定性合香名（前/中/后调）+ 印鉴（隐>雅>常 取深）+ 共振签文。
  - 邀请状态（`lib/inviteState.ts`）：base64 编解码，`markInviteeCompleted` 计数，满 3 人完成可享折扣（`canDiscount`）。
  - CP 图鉴（`/codex`，`lib/cpCodex.ts`）：16×16=256 格点亮矩阵，`markCpLit` 记录已点亮对。
- **边界异常**：单人访问（无好友结果）展示邀请态；`diffTones` 计算对 6 取最短距离；合香名为可交换排序（A|B 与 B|A 同结果）。
- **优先级**：P0（社交裂变核心）。

### 4.5 今日香签（Daily）— `components/DailyPanel.tsx` + `lib/daily/*`
- **描述**：每日确定性抽签 + 今日宜忌 + 香气历打卡 + 本命守护香 + 今日之瓶。
- **输入**：日期（`Asia/Shanghai`）；`localStorage` 香气历状态。
- **输出**：
  - `drawDaily(date)`：FNV-1a 哈希 → mulberry32 → Fisher-Yates，**全用户当日同签**；1 主香 + 2 灵感香；稀有度（常 60% / 雅 30% / 隐 10%）经由 `RARITY_LABEL`。
  - `drawAlmanac(date)`：今日宜（16 条情绪/审美向「宜」）/ 忌（16 条）/ 今日一语（8 条）——**合规，无吉凶占卜**。
  - `history.ts`：连续天数（streak，UTC 计）、冻结令牌（MAX 3，自动补缺口）、称号（初遇香气/初嗅≥7/沉香客≥30/一缕成香≥100/香气旧友≥365）、月历点阵。
- **业务规则**：抽签确定性依赖日期种子，**同日所有人结果一致**；香气历断签可用冻结令牌补救。
- **边界异常**：跨时区以 `Asia/Shanghai` 为准；冻结令牌耗尽后断签清零计数。
- **优先级**：P1（留存引擎）。

### 4.6 分享图渲染（Share Card）— `lib/shareCardRender.ts` + `app/api/share-card/route.ts`
- **描述**：服务端用 **satori（JSX→SVG）+ sharp（SVG→PNG）** 渲染分享图，支持 4 场景 × 2 比例。
- **输入**：`GET /api/share-card?scene=&format=&...`；`scene ∈ {self, friend, shared, daily}`，`format ∈ {1to1, 3to4}`（默认 1to1）。
- **输出**：`image/png`（带 `Cache-Control: max-age=300`）。
- **业务规则**：
  - **关键渲染约束**：satori 不支持嵌套 SVG 内 `<text>`，故中文标签必须用 satori 绝对定位 `<div>` 层渲染（`buildRadarLabels`）；雷达几何由 `buildRadarSVG/buildDualRadarSVG` 生成；自场景标签两行（名 + 0–100 数值），友场景双人不显示数值。
  - 网格/字号经多轮优化：自场景标签字号 ≈ 香水名（20px 级），网格色 `#9C7B47` 加深。
  - `scene=daily` 服务端确定性复用 `drawDaily/drawAlmanac`（与页面同源）。
- **边界异常**：参数缺失/格式非法返回 400；雷达 JSON 解析失败回退逗号分隔 6 值；非 0–100 分数报错。
- **优先级**：P0（分享闭环）。

### 4.7 支付墙（Payment Wall）— `lib/payment.ts`
- **描述**：解锁完整/进阶解读的付费门槛。
- **输入**：用户解锁意图；邀请完成状态。
- **输出**：`markPaid/getPaidLevel/hasPaid`（基于 `localStorage` `crushxiangjian_paid` 乐观解锁）；`initiatePayment()` 当前为**预留空实现，返回 `{ok:false}`**。
- **业务规则**：
  - 价格配置：`unlockDiscounted` ¥20.9（需邀请 3 人，`requiresReferral`）、`unlockFull` ¥29.9（主解锁价，折前 ¥59.9）。
  - `PAYMENT_QR` 为微信/支付宝二维码占位（待接入真实收款）。
  - `isWechatBrowser` 判断环境以切换支付引导。
- **边界异常**：**真实支付通道未接通**，当前仅为本地乐观	解锁，无服务端校验/订单/退款——属重大待办。
- **优先级**：P0（变现），但**实现状态为 P2（仅前端占位）**，【待确认】支付服务商与对账流程。
- **限时免费活动（2026-08-22 新增）**：`lib/payment.ts` 新增 `LIMITED_FREE` 开关（`enabled`、`endTime`，截止 `2026-09-30T23:59:59+08:00`）。活动期将「已解锁」判定合并入 `paidLevel >= 2`（即 `unlocked = paidLevel >= 2 || promoFree`），**隐藏整个付费墙**、隐藏「已解锁」徽章，改为常驻「限时免费开放中·倒计时」横幅；到期自动恢复付费墙，无需改动其余代码。分享图（teaser 锁定版）保持原样不随活动放开。

### 4.8 邀请/裂变状态 — `lib/inviteState.ts`
- **描述**：管理邀请码编解码、邀请人/被邀请人状态、折扣资格。
- **输入**：URL `?inv=`（base64 人格名）；本地邀请人/被邀请人列表。
- **输出**：`getInviteState/markInviteeCompleted/markInviteePending/setAsInviter`；`useInviteStatus`（含 focus + 2s 轮询）；`canDiscount`（≥3 完成）。
- **业务规则**：`encodeInvite/decodeInvite` base64；`encodeCpPair/decodeCpPair` 用 `~` 分隔双人。
- **边界异常**：base64 解码失败回退空邀请人；轮询仅在窗口聚焦时生效。
- **优先级**：P1（裂变）。

---

## 五、非目标（Out of Scope）

1. **不做后端服务 / 用户账户体系**：当前无任何服务端存储、登录、OpenID；用户数据仅 `localStorage`，换设备/清缓存即丢失。【待确认】远期是否需账号云同步。
2. **不做真实电商交易**：支付仅为前端占位，不接订单/库存/物流/售后。
3. **不做迷信占卜**：明确排除吉凶、转运、桃花、命定恋人等表述（见第十章）。
4. **不做医疗/法律/财务建议**：「今日宜忌」仅情绪与审美维度，不暗示健康/投资/法律风险。
5. **不做多语言/i18n**：当前界面全中文，无国际化框架。
6. **不做 PC 端深度适配**：以 ≤430px 移动端为主，桌面仅「可用」非「优化」。
7. **不做 A/B 实验平台 / 真实埋点系统**：当前无 analytics 接入，指标需【待确认】补埋点。

---

## 六、信息架构与核心用户流程

### 6.1 信息架构（站点地图）
```
/                         落地页（测香 / 今日香签 双模式）
├─ /question              人格测试（10 题，分支+校准）
├─ /result                结果页（人格解读 + 雷达 + 三香 + 守护香 + 支付墙 + 分享）
├─ /shared                好友结果分享视图（?p=人格，展示雷达+本命香+QR 回流）
├─ /friend                好友匹配（邀请态 / CP 预览 / 结果 / 手动 四态）
├─ /codex                CP 图鉴（16×16=256 点亮矩阵）
└─ /api/share-card        分享图渲染（self/friend/shared/daily × 1to1/3to4）
（对话/代码中提及页：/preview、/privacy、/terms、/api/og —— 存在但本次未逐一核读，【待确认】具体内容）
```

### 6.2 核心用户流程
**主漏斗（测香）**
```
落地页(测香) → 开始测试 → q1..q7 分支 → cal1..cal3 校准 → 正在调香(1.8s) → 结果页
   （带?inv=则 → /friend；带?cp=则 → /result?cp=）
```

**社交裂变（好友匹配）**
```
结果页「邀请好友」→ 生成邀请码/二维码 → 好友点开(/question?inv=) → 好友完成测试 →
markInviteeCompleted → 回 /friend 展示契合度 + 合香卡 + 分享图
```

**每日留存（今日香签）**
```
落地页(今日香签) → DailyPanel → 抽到当日签 + 宜忌 + 香气历打卡 → 次日回访累积 streak
```

**付费转化（支付墙）**
```
结果页解锁区 → 选择 ¥29.9 直接解锁 / 邀 3 人享 ¥20.9 → initiatePayment()(空) → localStorage 乐观解锁
```

---

## 七、关键页面/模块说明

| 页面/模块 | 关键组成 | 备注 |
|---|---|---|
| 落地页 | Hero 渐变 + 浮动光斑 + 香水瓶 SVG + 双行大标题 + 数据条 + 16 人格胶囊 + Social Proof「12,047 人」+ 主 CTA 脉冲 | 顶部胶囊切换测香/今日香签；已测用户显示人格胶囊 |
| 测试页 | 粘性顶栏（返回/步进器/跳过）+ 进度条 + 路径标签 + 题干 + 选项（A/B 编号圈）+ 底部主按钮 + 转场层 + 正在调香页 | 题间 300ms 转场文案强化叙事 |
| 结果页 | 人格头（mbti·主调·名·tagline）+ 雷达图 + 三香横排（本命/进阶/尝试，含匹配度）+ 守护香卡 + 记忆场景 + 支付墙 + 分享引导 | 含 `ShareGuideModal`、`ScentPreferenceBar`、`MemorySceneSection` |
| 好友匹配 | 邀请态 / CP 预览态(`?cp=`) / 结果态 / 手动态 四态机；评分圆环揭晓 + 香调对比雷达 + 共鸣气味 + 场景故事 + 互补解读 + 合香卡 + 分享图比例选择(1:1/3:4) | `markCpLit` 点亮图鉴 |
| CP 图鉴 | 256 格矩阵，已配对亮起 | `lib/cpCodex.ts` |
| 今日香签 | 抽签卡(主+2灵感, 稀有度印) + 宜忌 + 今日一语 + 香气历(连续天/冻结/称号/月历点阵) + 本命守护香 + 今日之瓶 | 确定性日签 |
| 分享图 | 服务端 PNG，覆盖 4 场景 2 比例；中文标签用 div 层（satori 约束） | 见 4.6 |

---

## 八、技术架构概述

### 8.1 技术栈（实证自 `package.json` / `README`）
| 层 | 技术 |
|---|---|
| 框架 | **Next.js 16**（App Router），React 19 |
| 语言 | TypeScript |
| 样式 | Tailwind（配置含 v3 `tailwind.config.ts`；README 称 v4，【待确认】实际版本口径） |
| 分享图渲染 | **satori 0.29**（JSX→SVG）+ **sharp 0.35.3**（SVG→PNG） |
| 图表 | recharts（结果页雷达）、自研 SVG 雷达（分享图） |
| 动画 | framer-motion |
| 二维码 | qrcode / api.qrserver.com |
| 部署 | GitHub Actions → Vercel；`serve.mjs` 反向代理 `/api/*` |

### 8.2 目录结构（关键）
```
app/
  page.tsx              落地页
  question/page.tsx     测试
  result/page.tsx       结果页
  shared/SharedViewClient.tsx
  friend/FriendView.tsx
  codex/page.tsx
  api/share-card/route.ts
  preview / privacy / terms / api/og  【待确认】未核读
lib/
  personalities.ts      16 人格定义、雷达、推荐、守护香、分享语
  matchPerfumes.ts      18 维香调匹配引擎（余弦相似 + 校准打分 + 平价去重）
  branchingQuestions.ts 10 题分支定义
  friendMatch.ts        契合度算法
  cpResonance.ts        合香共振
  cpCodex.ts            CP 图鉴
  inviteState.ts        邀请/裂变状态
  payment.ts            支付墙（占位）
  daily/{draw,almanac,history}.ts  今日香签
  data.ts               PERFUMES（香水库）、PERSONALITY_TYPES
  shareCardRender.ts    分享图渲染
components/             RadarChart / CpBlendCard / DailyPanel / GuardianScentCard / PaymentModal / UnlockedContent / ScentCodex / PersonalityIcon 等
```

### 8.3 核心数据模型
- **Perfume**（`lib/data.ts`）：`id / name / brand / brandCn / notes{top,heart,base} / tier('premium'|'budget') / priceRange / intensity / longevity / description`。`PERFUMES` 以「名称」为键的对象；`TOTAL_PERFUMES = Object.keys(PERFUMES).length`（动态）。
- **PersonalityType**：`id / name / mbti / tagline / description / direction(主调) / radarScores(6 维中文键 0–1) / scentDirection` 等；`PERSONALITY_TYPES` 16 条；`PERSONALITY_ID_MAP`（中文名→英文 slug）。
- **ScentVector / ScentProfile18D**（`matchPerfumes.ts`）：6 维英文键（floral/woody/fresh/oriental/citrus/gourmand）；18 维 = 前/中/后调各 6 维。
- **CalibratedRecommendation**：`name/brand/brandCn/notes/notesStructured/quote/tier/role('signature'|'advanced'|'budget')/match/priceRange/intensity/longevity/score`。

### 8.4 核心算法/接口
- **人格判定**：`calculatePersonalityFromPath(choices)`（分支路径 → 16 人格之一）。
- **推荐引擎**：`getCalibratedRecommendations(radar, calChoices, pathLabels, archetypeId?, useArchetypeBudget?)`：
  - 综合得分 = 雷达 0.45（18D×50% + 6D×50%）+ 校准气息 0.15 + 留香 0.20 + 场合 0.15 + 描述 0.05。
  - 本命香匹配度映射至 85–95%（`mapSignatureMatch`）；进阶香按单 ml 价 ≥¥20 门槛；尝试香按 27 校准方向去重 + 垄断去偏（BMK=9）+ MMR（λ=0.10）。
  - 16 原型 canonical 视图走 `buildArchetypeBudgetAssignment` 保证互不重复。
- **匹配**：`calculateCompatibility(me, friend)` = 共振 0.7 + 互补 0.3 + 基础 20，0–100。
- **合香**：`getCpResonance(A,B)` 经 6 音环 `diffTones` + 确定性合香名。
- **接口**：`GET /api/share-card`（见 4.6）；`serve.mjs` 代理 `/api/*`。

---

## 九、设计风格与规范

> 来源：代码中的 Tailwind 色板、`BrandSymbol` 组件、README 品牌规范。

| 项 | 规范 |
|---|---|
| 品牌符号 | **CRUSH FRAGRANCE** 文字标 + 上下琥珀色渐变细线（两端透明） |
| 主色 | 深琥珀 `#2C1810`、暖琥珀 `#D4A574`、米白 `#FAF3EA`（README 另记 `#A8884E` 金、`#FAF3EA` 米白、`#2C1810` 墨，需统一） |
| 字体 | 标题 **Noto Serif SC**（衬线）；正文 **Noto Sans SC**（无衬线） |
| 调性 | 克制、高级、仪式感；拒绝砍价拼团式低价裂变视觉 |
| 视觉元素 | 香水瓶 SVG 线稿、浮动光斑、噪点滤镜（`feTurbulence`）、脉冲 CTA、转场文案叙事 |
| 稀有度「以印代框」 | 常香无印 / 雅香金印「雅」/ 隐香印 + 笺底加深 |
| 移动端约束 | 容器 `max-w-[430px]`，安全区 `var(--safe-bottom)` |

> ⚠️ **口径不一致待修**：香水总数在 README（151/225）、代码注释（「151 支香水库」）、分享页 footer（「110支香水库」）三处不同；品牌金在 README 与落地页（用 `#A8884E`）写法不同。建议统一为单一数据源（以 `TOTAL_PERFUMES` 动态值为准）。

---

## 十、风险、合规与隐私

### 10.1 合规基线（产品核心红线，代码实证）
- **「玄学的壳，审美的核」**：以 16 人格体系替代星座/塔罗作为「玄学」外壳，内核始终是情绪与审美，平台更安全。
- **今日宜忌合规**：`drawAlmanac` 的「宜/忌」仅情绪状态与审美建议（如「宜独处」「忌喧闹」），**不含吉凶、转运、招桃花、破灾、灵验、命中注定**等迷信表述；不暗示医疗/法律/财务（如「今日不宜投资」踩线）。
- **视觉避讳**：避开塔罗牌/星盘/八卦/符咒等强迷信符号，防平台内容审核限流。
- **分享非诱导**：付费只卖「解读深度/内容」，不卖「概率/开运/护身」；分享是结果的自然延伸，非强制解锁条件（规避微信诱导分享红线）。

### 10.2 风险清单
| 风险 | 等级 | 说明 / 缓解 |
|---|---|---|
| **支付未接通** | 高 | `initiatePayment()` 返回 `{ok:false}`，无真实收款/订单/对账；当前仅靠 `localStorage` 乐观解锁，易被绕过且无营收。需【待确认】接入服务商 + 服务端校验。 |
| **无后端/无账号** | 中 | 用户数据全在 `localStorage`，换设备/清缓存即丢失，无法做真实留存/营收分析。 |
| **数据口径混乱** | 中 | 香水数 151/225/110 三处不一致；品牌色多处写法。需统一。 |
| **分享图渲染依赖** | 中 | satori/sharp 在服务端渲染，中文字体（Noto Serif/Sans SC）需正确加载，否则出现「豆腐块」乱码（历史上已发生并修复）。 |
| **内容审核限流** | 中 | 香签/签文若措辞不慎易触发平台迷信审核；需持续守「玄学的壳，审美的核」。 |
| **裂变合规** | 中 | 「邀请 3 人享折扣」需避免被判定为诱导分享/多级分销；当前为本地计数，无服务端风控。 |
| **性能/弱网** | 低 | 纯前端 + 大量香水数据 + SSR 分享图，移动端首屏与渲染耗时需压测【待确认】。 |
| **IP/数据合规** | 低 | 香水数据来源与版权【待确认】；隐私政策页 `/privacy`、条款页 `/terms` 存在但内容未核读。 |

### 10.3 隐私
- 当前**不采集任何 PII**：无注册、无表单、无服务端日志用户身份；仅 `localStorage` 存自测结果与邀请状态。
- 分享图经服务端 渲染但不持久化用户输入（仅按 query 即时渲染、缓存 300s）。
- **页脚合规链路变更（2026-08-22）**：已从全局页脚移除「定价说明 / 联系方式 / 隐私政策 / 服务条款」入口，仅保留版权行。当前纯前端、无 PII 采集，风险低；但 `/privacy`、`/terms` 页面仍存在，构成「有页面无入口」冗余。**若后续接入支付/后端，必须在页脚恢复隐私政策与用户协议入口**，否则不满足主流平台对交易类页面的合规要求。
- 【待确认】若接入真实支付/账号，需补充隐私政策与数据最小化设计。

### 10.4 合规审查清单（2026-08-22 全量扫描）

> 方法：对 `lib/`、`app/`、`components/` 做红线词扫描（转运/改命/破灾/招桃花/塔罗/星盘/八卦/符咒/开运/护身/算命/占卜/风水/命盘/命理），并逐读关键用户可见文案池（宜忌、CP 评级、守护签文、分享语）。

**✅ 通过项（实证）**
- **红线词扫描**：源码无任何命中（仅在合规注释中作为「规避说明」出现），即无实际迷信表述。
- **今日宜忌**（`lib/daily/almanac.ts`）：宜/忌/今日一语全为情绪与审美语境（如「宜留白」「忌急着要答案」），**无吉凶、无「宜投资/忌出行」等财务/出行/医疗暗示**。
- **守护香 / 签文 / 共振**（`personalities.ts`、`cpResonance.ts`、`GuardianScentCard`、`ScentCodex`）：均带「非命理断言」注释，内核为气味契合，无吉凶/转运/桃花。
- **付费合规**：仅卖「解读深度/内容」；本次「限时免费」活动隐藏付费墙、不强制分享解锁，延续「分享非诱导」红线；`app/terms/page.tsx` 已含「娱乐性参考内容，不构成医学、心理学、婚恋或投资建议」免责声明。

**⚠️ 建议优化项（未改动，供决策）**
1. **CP 契合度等级**：当前用「灵魂伴侣 / 天生一对」等词，虽未出现「命定恋人」字面，但语义偏浪漫宿命，接近「缘分宿命」。建议降级为「气息同频 / 互补共振 / 各有千秋」等审美表达，进一步规避微信对「交友/相亲」类内容的审核风险（契合度仍是 0–100 分数机制，仅文案需调整）。
2. **「治愈」用词**：作为香气情绪词在 `data.ts`/`personalities.ts` 广泛出现，属审美描述；若未来接入健康/医疗语境需注意不被误解为疗效暗示。
3. **页脚合规入口缺失**：见 10.3；`/privacy`、`/terms` 页面存在但无入口，建议二选一（恢复入口 / 删除页面）避免内容过时。
4. **限时免费横幅**：建议在横幅旁补充一行轻量免责「测试结果为娱乐性参考」，与 `/terms` 口径保持一致。

**🔧 已修复（历史 + 本次）**
- 分享图雷达中文乱码/字号过大（已修复：div 层 + 20px 级字号 + 网格加深）。
- 首页/结果页/分享页「隔 0 调」→「同调」（文案修正）。
- 页脚冗余合规链接移除（本次）。

---

## 十一、术语表与附录

### 11.1 术语表
| 术语 | 含义 |
|---|---|
| 本命香 | 推荐三档中的 top1（premium 池），匹配度映射 85–95% |
| 进阶香 | 三档中第 2 款（高价 ≥¥20/ml 的 premium），真实匹配分 |
| 尝试香 | 三档中第 3 款（budget 池），校准方向去重 + MMR |
| 契合度 | 好友匹配评分 0–100（共振 0.7 + 互补 0.3 + 基础 20） |
| 合香 / CP 共振 | 双人香调合成确定性「合香名」（前/中/后调）+ 印鉴 |
| 隔 N 调 / 同调 | 双人香调在 6 音环的最短距离；0 即为「同调」 |
| 香气历 | 今日香签连续访问打卡体系（streak + 冻结令牌 + 称号） |
| 稀有度（以印代框） | 常/雅/隐 三档，对应 60%/30%/10% 抽签概率 |
| 校准题 | cal1（气息偏向）/ cal2（留香偏好）/ cal3（使用场景） |
| 18 维香调 | 前/中/后调各 6 维（floral/woody/fresh/oriental/citrus/gourmand） |
| BMK / MMR | 平价档垄断去偏强度(9) / 最大边际相关去相似(λ=0.10) |
| satori / sharp | 分享图渲染链路：JSX→SVG→PNG |

### 11.2 附录 A：已知遗留 / 待办（代码实证）
- 支付接口为空实现，需接入真实服务商 + 服务端校验。
- 香水总数口径三处不一致，需统一为 `TOTAL_PERFUMES` 动态值并修正文案。
- 分享页 footer「110支香水库」与 README/数据不符，建议改为动态值。
- `/preview`、`/privacy`、`/terms`、`/api/og` 页面存在但本次未逐一核读，【待确认】内容完整性。
- 雷达图中文标签渲染曾出现乱码/字号过大，已修复（div 层 + 20px 级字号 + 网格加深）。

### 11.3 附录 B：本 PRD 的【待确认】汇总
1. 业务立项动机与是否为品牌/电商导流 —— 无文档佐证。
2. 真实用户规模、营收、转化率等基线数据 —— 仅有静态占位「12,047 人」。
3. 支付服务商、对账、退款、风控流程 —— 接口为空。
4. 远期是否需要账号云同步/后端 —— 当前纯前端。
5. 香水数据源版权与是否含真实电商 SKU —— 【待确认】。
6. `/preview`、`/privacy`、`/terms`、`/api/og` 页面具体内容 —— 未核读。
7. Tailwind 实际版本（v3 配置 vs README v4 表述）—— 口径待统一。

---

*文档基于 2026-08 期间对话历史与 Crush 香鉴 Next.js 16 工程代码逆向整理；功能与交互均经代码实证，未实证的产品/商业意图已标注【待确认】。*
